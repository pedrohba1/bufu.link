{
  "name": "bufu.link",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "svelte-kit sync || echo ''",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "pdf:svgs": "node scripts/pdfs-to-svgs.mjs"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^7.0.0",
    "@sveltejs/kit": "^2.48.5",
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.43.8",
    "svelte-check": "^4.3.4",
    "typescript": "^5.9.3",
    "svg-path-bounds": "^1.0.1",
    "svgpath": "^2.6.0",
    "svgo": "^4.0.0",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
